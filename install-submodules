#!/bin/bash
BASE_DIR=$HOME

echo "Base DIR: $BASE_DIR"

# Install oh-my-zsh
sh -c "$(curl -fsSL https://raw.githubusercontent.com/robbyrussell/oh-my-zsh/master/tools/install.sh)"

git clone https://github.com/gmarik/Vundle.vim.git          $BASE_DIR/vim.config/vim/bundle/Vundle.vim

sh ./osx.config/brew.sh
sh ./osx.config/osx.sh

ln -s $BASE_DIR/vim.config/vim $HOME/.vim
ln -s $BASE_DIR/tmux.config/tmux.conf $HOME/.tmux.conf

# Install neovim config
git clone https://github.com/rafi/vim-config.git ./neovim.config
mkdir $BASE_DIR/.config
ln -s $BASE_DIR/dotfiles/neovim.config $BASE_DIR/.config/nvim
ln -s $BASE_DIR/dotfiles/neovim.config $BASE_DIR/.vim 
pip2 install -U neovim PyYAML
pip3 install -U neovim PyYAML
cd ./neovim.config && make

cd $BASE_DIR

echo "Put 'source $BASE_DIR/bash.config/main.sh' in .zshrc"
